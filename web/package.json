{
  "name": "@opendataworld/fabric",
  "version": "0.1.0-alpha",
  "description": "Single-language (TypeScript) client for the OpenDataWorld data fabric — talks directly to SurrealDB.",
  "type": "module",
  "main": "fabric.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "demo": "tsx demo.ts"
  },
  "dependencies": {
    "surrealdb": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.0.0"
  }
}
